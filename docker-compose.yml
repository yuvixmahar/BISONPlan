services:
  api:
    build:
      context: .
      target: prod
    ports:
      - "8000:8000"
    environment:
      CORS_ORIGINS: "http://localhost:5173,http://localhost:4173"
      PORT: "8000"
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
